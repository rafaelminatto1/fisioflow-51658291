# ========================================
# Configurações do macOS Docker
# FisioFlow iOS Development
# ========================================

# VNC Configuration
# Senha para acessar o macOS via VNC
VNC_PASSWORD=fisioflow123

# Portas
VNC_PORT=8888          # VNC Web (navegador)
VNC_NATIVE=5900        # VNC Nativo (cliente VNC)
SSH_PORT=2222          # SSH (opcional)

# Hardware Allocation
# ⚠️  Ajuste conforme seu sistema
# RAM_SIZE: Recomendado 8g mínimo, 16g ideal
# CPU_CORES: Recomendado 4 mínimo, 8 ideal
RAM_SIZE=8g
CPU_CORES=4

# Disk Size
DISK_SIZE=128g

# Display
DISPLAY=:1

# Configurações avançadas
# CPU_FLAGS="-enable-kvm -m 8192 -smp 4,cores=4,sockets=1"
