/**
 * Expo Entry Point
 * Este arquivo é o ponto de entrada para o app Expo
 * Ele registra componentes e configura o app para mobile
 */

import { registerRootComponent } from 'expo';
import App from './App';

// Registrar o componente principal
registerRootComponent(App);
