{
  "version": "7.3",
  "data_api_version": "3.0",
  "routing_model": { "APPOINTMENT": [] },
  "screens": [
    {
      "id": "APPOINTMENT",
      "title": "Agendar atendimento",
      "terminal": true,
      "data": {
        "types": { "type": "array", "items": { "type": "object", "properties": { "id": { "type": "string" }, "title": { "type": "string" } } }, "__example__": [{ "id": "evaluation", "title": "Avaliação" }, { "id": "session", "title": "Sessão" }] },
        "periods": { "type": "array", "items": { "type": "object", "properties": { "id": { "type": "string" }, "title": { "type": "string" } } }, "__example__": [{ "id": "manha", "title": "Manhã (07h às 12h)" }, { "id": "tarde_noite", "title": "Tarde / Noite (13h às 20h)" }] },
        "slots": { "type": "array", "items": { "type": "object", "properties": { "id": { "type": "string" }, "title": { "type": "string" } } }, "__example__": [{ "id": "08:00", "title": "08:00" }] }
      },
      "layout": {
        "type": "SingleColumnLayout",
        "children": [
          {
            "type": "Form",
            "name": "form",
            "children": [
              { "type": "Dropdown", "name": "type", "label": "Tipo de atendimento", "required": true, "data-source": "${data.types}" },
              { "type": "DatePicker", "name": "date", "label": "Data", "required": true, "on-select-action": { "name": "data_exchange", "payload": { "type": "${form.type}", "date": "${form.date}", "period": "${form.period}" } } },
              { "type": "Dropdown", "name": "period", "label": "Período", "required": true, "data-source": "${data.periods}", "on-select-action": { "name": "data_exchange", "payload": { "type": "${form.type}", "date": "${form.date}", "period": "${form.period}" } } },
              { "type": "Dropdown", "name": "slot", "label": "Horário disponível", "required": true, "data-source": "${data.slots}" },
              { "type": "Footer", "label": "Confirmar", "on-click-action": { "name": "complete", "payload": { "type": "${form.type}", "date": "${form.date}", "period": "${form.period}", "slot": "${form.slot}" } } }
            ]
          }
        ]
      }
    }
  ]
}
