# Necessário para Expo prebuild + CocoaPods em pnpm workspace
# CocoaPods resolve node_modules de forma flat; sem isso, react-native-worklets
# e outros pacotes nativos não são encontrados
shamefully-hoist=true
