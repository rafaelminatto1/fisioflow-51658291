/**
 * STUB temporário — componente original ausente (pasta components/dashboard/ vazia),
 * quebrava o bundle do app inteiro (import em app/(tabs)/index.tsx).
 * Reimplementar com os KPIs reais de saúde da clínica.
 */
export function ClinicHealthKPIs() {
  return null;
}

export default ClinicHealthKPIs;
