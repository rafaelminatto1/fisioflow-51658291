# Jules PR Bot - Cloudflare Worker configuration
name = "jules-bot"
main = "src/index.ts"
compatibility_date = "2024-11-06"

[vars]
ENVIRONMENT = "production"

# Note: GITHUB_TOKEN, GEMINI_API_KEY, and WEBHOOK_SECRET should be set as secrets
# using `wrangler secret put <NAME>`
