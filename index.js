/**
 * FisioFlow - Expo Entry Point
 * This file serves as the entry point for Expo/React Native
 */

import { registerRootComponent } from 'expo';
import App from './App';

// Register the main app component
registerRootComponent(App);
