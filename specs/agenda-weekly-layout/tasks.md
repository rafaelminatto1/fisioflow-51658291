# Tasks: Agenda Semanal e Cards de Agendamento Soft

1. Criar `WeeklyScheduleSummary` para mostrar período semanal e métricas.
2. Importar e renderizar `WeeklyScheduleSummary` em `ScheduleCalendar` para `viewType === "week"`.
3. Refatorar `ScheduleEventContent` para aplicar bordas arredondadas, manchas suaves de cor e tipografia mais elegante.
4. Ajustar `src/styles/schedule.css` com regras específicas para visualização semanal e eventos FullCalendar.
5. Adicionar teste unitário `WeeklyScheduleSummary.test.tsx`.
6. Revisar manualmente a página `/schedule` para garantir que o novo layout não quebre as interações existentes.
