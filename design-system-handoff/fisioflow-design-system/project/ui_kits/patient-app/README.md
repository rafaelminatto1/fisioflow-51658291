# UI Kit — Patient App (Mobile)

App do paciente em React Native (Expo). Recreated in HTML/JSX inside an iOS frame using the design system tokens.

Screens: Home (próxima sessão + protocolo do dia), Exercício individual com timer, Evolução (pain log).
