{
  "mcpServers": {
    "playwright": {
      "command": "npx",
      "args": ["-y", "@playwright/mcp@latest"],
      "description": "Local Playwright MCP server for browser automation"
    },
    "cloudflare-api": {
      "httpUrl": "https://mcp.cloudflare.com",
      "description": "Cloudflare managed MCP server for the Cloudflare API",
      "timeout": 600000
    },
    "expo-mcp": {
      "httpUrl": "https://mcp.expo.dev/mcp",
      "description": "Expo hosted MCP server for Expo and EAS workflows",
      "timeout": 600000
    }
  }
}
