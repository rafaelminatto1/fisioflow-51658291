/**
 * RecipeLibrary Component (Stub)
 */

import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export function RecipeLibrary() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Biblioteca de Receitas</CardTitle>
      </CardHeader>
      <CardContent>
        <p className="text-muted-foreground">Biblioteca de receitas de automação em desenvolvimento...</p>
      </CardContent>
    </Card>
  );
}
