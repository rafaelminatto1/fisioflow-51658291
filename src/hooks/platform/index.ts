/**
 * Platform Hooks - Index
 *
 * Exporta todos os hooks relacionados à detecção de plataforma
 */

export { usePlatform, getPlatform, selectPlatform } from './usePlatform';
export type { PlatformType, PlatformInfo } from './usePlatform';
