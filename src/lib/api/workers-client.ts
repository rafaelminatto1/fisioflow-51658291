/**
 * Camada de compatibilidade legada.
 *
 * O app web passou a consumir `@/api/v2` diretamente.
 * Este arquivo permanece apenas para imports externos ou migrações graduais.
 */
export * from "@/api/v2";
