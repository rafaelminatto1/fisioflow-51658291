/**
 * Database Constants and Schema Documentation
 *
 * This module provides:
 * 1. Type-safe constants for all database status values
 * 2. Validation functions for status values
 * 3. Database schema documentation
 *
 * @see {@link ./constants.ts} for status constants
 * @see {@link ./schema.ts} for schema documentation
 * @see {@link ./validation.ts} for validation utilities
 */

export * from './constants';
export * from './schema';
export * from './validation';
